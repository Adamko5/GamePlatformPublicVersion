A single manifest tells the platform what it needs to know about routing, health checks, capabilities and catalog metadata without exposing the game internals.

Example: [`game.manifest.json`](./game.manifest.json)

All values below are fictional.
