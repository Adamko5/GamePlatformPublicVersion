Each game defines its own theme, while the shared SDK applies the same contract.

The framework owns the plumbing. A game supplies tokens for color, shape, typography, motion and effects.

This keeps the platform reusable without making every game look the same.

Example: [`example-theme.ts`](./example-theme.ts)

All values below are fictional.
